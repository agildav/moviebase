{>"layouts/master" /}

{<body}
    {#movies}
      <div>
        <img class="border" src="{.cover}">
        <h3>{.title}</h3>
        <a class="btn btn-info" href="/movies/details/{._id}">Info</a>
      </div>
      <br>
    {:else}
      <p>No Movie Results</p>
    {/movies}
{/body}
