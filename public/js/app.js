/*global requirejs:true*/
"use strict";

requirejs.config({
    paths: {}
});

require([
    /* Dependencies */
], function() {
    var app = {
        initialize: function() {}
    };

    app.initialize();
});
